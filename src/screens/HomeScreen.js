import React from 'react';
import { View, Text } from 'react-native';

const HomeScreen = () => {
  return (
    <View>
      <Text>Page d'Accueil (Météo)</Text>
      {/* Ajoute ici des composants liés à la météo */}
    </View>
  );
};

export default HomeScreen;